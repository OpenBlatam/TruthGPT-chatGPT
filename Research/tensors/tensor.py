import numpy as np

# Create an array with the desired shape and data type, with all elements initialized to 0.0
shape = (1000, 1000)
dtype = np.float64
arr = np.zeros(shape, dtype=dtype)

# Print the array
print(arr)
